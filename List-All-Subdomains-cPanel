echo "Cpanel List All Subdomains script by Ayhan ARDA - ayhanarda.com" && echo start && for i in $(ls /var/cpanel/users); do /scripts/listsubdomains $i;done | grep -v ":" > test.txt && sleep 1 && awk '{print substr($0,2)}' test.txt
